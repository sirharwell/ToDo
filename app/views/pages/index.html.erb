<h1>Items</h1></h1>

<% @pages.each do |page| %>
  <h5>What:  <%= page.title %> </br>
  Details:  <%= page.body %></h5>
    <%= link_to 'Edit', edit_page_path(page) %>
    <%= link_to 'Delete', page_path(page), method: :delete %>
<% end %>

</br>
</br>
</br>

<button class="btn black white-text"><%= link_to 'Create New Item', new_page_path %></button>
